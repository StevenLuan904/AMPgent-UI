# AMPgent UI 协作规则

本文件适用于此仓库中的所有后续改动。

## 提交与分支

- 唯一交付仓库为 `https://github.com/StevenLuan904/AMPgent-UI.git`。
- `develop` 是默认集成分支。日常修复和范围明确的小型优化在验证后直接提交并推送到 `develop`。
- 重大需求必须从最新的 `develop` 自主创建短期分支 `agent/<scope>`，完成后推送并创建合并至 `develop` 的拉取请求，无需等待额外指令。
- 新增或大幅重构工作流、页面体系、数据契约、数据库结构、系统架构或跨页面设计系统，均视为重大需求。
- 除非用户明确要求发布晋级，不向 `main` 提交新功能。
- 提交前检查工作树和暂存差异，不提交密钥、环境变量、数据库、缓存、构建产物或机器专属配置。
- 禁止强制推送共享分支或改写已发布历史。

## 交付验证

- 提交前至少运行 `npm run build`。
- 涉及界面或交互的改动，应运行本地应用并使用浏览器截图检查布局、可读性、溢出和主要交互。
- 无法执行的检查必须在交付说明或拉取请求中明确记录。

## 运行事实源与恢复

- 远端 PostgreSQL 是唯一权威科学事实源；工作站经受监督隧道 `127.0.0.1:55432` 只读访问，服务端入口为 `192.168.99.19:55433`。凭据仅由外部安全配置加载，不写入仓库、日志或界面。
- UI 只读取 Observer／分析接口，不直连 Codex、Temporal 或数据库；科学运行状态与调度可观测性分别呈现，不以不可观测推断科学失败。
- 当前数据库为 PostgreSQL 16.4、Alembic `0017_artifact_location_witnesses`；历史精确重放按后端展示契约排除，UI 不自行猜测或合并运行身份。
- 禁止为展示或恢复而重启现有任务、重复提交工作流或改写历史记录；断线时等待既有隧道恢复。
- 恢复入口为 `192.168.99.19:55434` 的隔离验证实例；备份同时保存在 `.19` 与 `synth .2`，恢复操作以外部审计收据为准，不把恢复实例切换为生产事实源。
